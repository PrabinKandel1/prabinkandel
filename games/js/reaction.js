const g=$("#game");g.innerHTML=`<div class="game-hud"><div class="hud-item"><span>LAST</span><strong id="last">—</strong></div><div class="hud-item"><span>BEST</span><strong id="best">${localStorage.getItem("pk-reaction-best")||"—"}</strong></div><div class="hud-item"><span>AVERAGE</span><strong id="avg">—</strong></div></div><button id="zone" class="game-button" type="button">Start reaction test</button><div class="result" id="result">Do not click before the signal.</div>`;const z=$("#zone"),r=$("#result"),last=$("#last"),avg=$("#avg");z.classList.add("reaction-zone");let state="idle",go=0,times=[];let timer;z.onclick=()=>{if(state==="idle"){state="wait";z.textContent="WAIT…";z.classList.add("reaction-wait");timer=setTimeout(()=>{state="go";go=performance.now();z.textContent="CLICK NOW";z.classList.remove("reaction-wait");z.classList.add("reaction-go")},rand(1200,3500))}else if(state==="wait"){clearTimeout(timer);state="idle";z.classList.remove("reaction-wait");z.textContent="Too early — false start";r.textContent="False start. Try again.";setTimeout(()=>z.textContent="Start reaction test",900)}else{const ms=Math.round(performance.now()-go);times.push(ms);const b=best("pk-reaction-best",ms,true);last.textContent=ms+" ms";avg.textContent=Math.round(times.reduce((a,b)=>a+b,0)/times.length)+" ms";r.textContent=`${ms} ms${b?" · New personal best.":""}`;state="idle";z.classList.remove("reaction-go");z.textContent="Try again"}};
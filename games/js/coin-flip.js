const root=document.querySelector("#game-ui");let rounds=0,wins=0,choice="";
root.innerHTML=`<div class="game-hud"><div class="hud-item"><span>ROUNDS</span><strong id="rounds">0</strong></div><div class="hud-item"><span>WINS</span><strong id="wins">0</strong></div><div class="hud-item"><span>RATE</span><strong id="rate">0%</strong></div></div><div class="coin-stage"><div class="coin" id="coin">?</div></div><div class="choices"><button class="choice" data-call="HEADS">Heads</button><button class="choice" data-call="TAILS">Tails</button></div><div class="result" id="result">Make a call, then flip.</div>`;
const coin=document.querySelector("#coin"),result=document.querySelector("#result");function flip(call){choice=call;rounds++;const outcome=Math.random()>.5?"HEADS":"TAILS";if(outcome===call)wins++;coin.classList.remove("spin");void coin.offsetWidth;coin.classList.add("spin");coin.textContent=outcome;const rate=Math.round(wins/rounds*100);document.querySelector("#rounds").textContent=rounds;document.querySelector("#wins").textContent=wins;document.querySelector("#rate").textContent=rate+"%";result.textContent=outcome===call?`Correct — ${outcome}. Win rate ${rate}%.`:`Miss — it was ${outcome}. Win rate ${rate}%.`;}
document.querySelectorAll("[data-call]").forEach(b=>b.onclick=()=>flip(b.dataset.call));
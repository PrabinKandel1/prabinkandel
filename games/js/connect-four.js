const root=document.querySelector("#game-ui");let b=Array(42).fill(0),turn=1,over=false;function win(p){for(let r=0;r<6;r++)for(let c=0;c<7;c++){let i=r*7+c;if(c<4&&[1,2,3].every(k=>b[i+k]===p)||r<3&&[1,2,3].every(k=>b[i+k*7]===p)||r<3&&c<4&&[1,2,3].every(k=>b[i+k*8]===p)||r<3&&c>=3&&[1,2,3].every(k=>b[i+k*6]===p))return true}return false}function play(c){if(over)return;for(let r=5;r>=0;r--){let i=r*7+c;if(!b[i]){b[i]=turn;if(win(turn)){over=true;m.textContent=`PLAYER ${turn} WINS.`}else if(b.every(Boolean)){over=true;m.textContent="DRAW — full board."}else turn=turn===1?2:1;draw();return}}}function draw(){grid.innerHTML=b.map((v,i)=>`<button class="${v?'p'+v:''}" aria-label="Column ${i%7+1}"></button>`).join("");grid.querySelectorAll("button").forEach((x,i)=>x.onclick=()=>play(i%7));if(!over)m.textContent=`PLAYER ${turn} · choose a column.`}root.innerHTML=`<div class="toolbar"><span class="score">TWO PLAYERS · 7 × 6</span><button class="btn primary" id="new">New match</button></div><div class="connect" id="grid"></div><div class="message" id="m"></div>`;const grid=document.querySelector("#grid"),m=document.querySelector("#m");document.querySelector("#new").onclick=()=>{b=Array(42).fill(0);turn=1;over=false;draw()};draw();
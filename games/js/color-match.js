const colors=[["RED","c0"],["GREEN","c1"],["BLUE","c2"],["AMBER","c3"],["TEAL","c4"],["PURPLE","c5"]];const g=$("#game");g.innerHTML=`<div class="game-hud"><div class="hud-item"><span>ROUND</span><strong id="round">0</strong></div><div class="hud-item"><span>SCORE</span><strong id="score">0</strong></div><div class="hud-item"><span>BEST</span><strong id="best">${localStorage.getItem("pk-color-best")||"0"}</strong></div></div><h2 id="target"></h2><div class="choices" id="choices"></div><div class="result" id="result"></div>`;let score=0,round=0,answer="";function q(){round++;$("#round").textContent=round;const x=colors[rand(0,colors.length-1)];answer=x[0];$("#target").textContent=`Choose ${answer}`;$("#choices").innerHTML=shuffle([...colors]).map(c=>`<button class="choice ${c[1]}" data-c="${c[0]}" type="button">${c[0]}</button>`).join("");$("#choices").querySelectorAll("button").forEach(b=>b.onclick=()=>{if(b.dataset.c===answer){score++;$("#score").textContent=score;const n=best("pk-color-best",score);$("#result").textContent=n?"New personal best.":""}else {$("#result").textContent="Wrong match — try another option.";return}})}q();
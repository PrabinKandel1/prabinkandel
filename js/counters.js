export function initCounters(){const els=[...document.querySelectorAll("[data-counter]")];const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting)return;const el=e.target;if(el.dataset.done)return;el.dataset.done="1";const target=Number(el.dataset.counter),dec=Number(el.dataset.decimals||0),suffix=el.dataset.suffix||"",prefix=el.dataset.prefix||"";const start=performance.now(),dur=1100;const step=t=>{const p=Math.min(1,(t-start)/dur),v=target*(1-Math.pow(1-p,3));el.textContent=prefix+v.toFixed(dec)+suffix;if(p<1)requestAnimationFrame(step)};requestAnimationFrame(step);io.unobserve(el)}),{threshold:.6});els.forEach(e=>io.observe(e));}
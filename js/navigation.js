import {$,$$} from "./utils.js";export function initNavigation(){const header=$("#site-header"),menu=$("#menu-toggle"),nav=$("#mobile-nav"),links=$$(".desktop-nav a, .mobile-nav a");window.addEventListener("scroll",()=>{header.classList.toggle("scrolled",scrollY>30);const h=document.documentElement,ratio=scrollY/(h.scrollHeight-innerHeight);$("#scroll-progress").style.width=(ratio*100)+"%";let active="";$$("main section[id]").forEach(s=>{if(scrollY>=s.offsetTop-180)active=s.id});$$(".desktop-nav a").forEach(a=>a.classList.toggle("active",a.getAttribute("href")==="#"+active))},{passive:true});menu?.addEventListener("click",()=>{const open=menu.getAttribute("aria-expanded")==="true";menu.setAttribute("aria-expanded",String(!open));nav.hidden=open});links.forEach(a=>a.addEventListener("click",()=>{nav.hidden=true;menu?.setAttribute("aria-expanded","false")}));}
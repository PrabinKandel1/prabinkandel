export const $=(s,r=document)=>r.querySelector(s);export const $$=(s,r=document)=>[...r.querySelectorAll(s)];export const clamp=(n,min,max)=>Math.min(max,Math.max(min,n));export const qs=key=>new URLSearchParams(location.search).get(key);export const storage={get(k,f=null){try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}},set(k,v){try{localStorage.setItem(k,JSON.stringify(v))}catch{}}};